# GitHub token used by star ingestion jobs.
# Create a Fine-grained personal access token and paste it here.
# This can be left blank if you only run occasionally, but rate limits will be much lower.
GITHUB_TOKEN=

# Optional: enable full stargazer-event backfill in recompute_history.
GITHUB_BACKFILL=0

# Optional API/server settings.
COMMUNITY_METRICS_API_HOST=127.0.0.1
COMMUNITY_METRICS_API_PORT=8000
COMMUNITY_METRICS_TIMEOUT_SECONDS=30

# LanceDB Enterprise (required for all ingestion/API scripts)
LANCEDB_API_KEY=
LANCEDB_HOST_OVERRIDE=
LANCEDB_REGION="us-east-1"
